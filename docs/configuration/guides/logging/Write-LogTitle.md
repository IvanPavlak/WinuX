# Write-LogTitle

Writes a section header in the house `[Title]` style (DarkCyan), replacing the `Write-Host -ForegroundColor DarkCyan "`n[Title]"` idiom.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Write-LogTitle "Kill All"
```

## Related

- [`Write-LogTitle` in the Logging module reference](../../../modules/logging.md#write-logtitle) - parameters, usage and behaviour
- [Logging configuration guides](README.md) - every guide for this module
