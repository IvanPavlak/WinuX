# Write-LogSuccess

Writes a success message in the house `=> ` style (Green), replacing the `Write-Host -ForegroundColor Green "`n=> ..."`idiom.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Write-LogSuccess "Workspace opened successfully!"
```

## Related

- [`Write-LogSuccess` in the Logging module reference](../../../modules/logging.md#write-logsuccess) - parameters, usage and behaviour
- [Logging configuration guides](README.md) - every guide for this module
