# Install-WinGetPackageManager

Installs the WinGet package manager if it is not already present.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Install-WinGetPackageManager
```

## Related

- [`Install-WinGetPackageManager` in the Bootstrap module reference](../../../modules/bootstrap.md#install-wingetpackagemanager) - parameters, usage and behaviour
- [Bootstrap configuration guides](README.md) - every guide for this module
