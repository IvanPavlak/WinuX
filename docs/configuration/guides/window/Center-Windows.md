# Center-Windows

Centers all open windows on their respective monitors.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Center-Windows
Center-Windows -WidthPercent 60 -HeightPercent 70
Center-Windows -ProcessName "chrome"
```

## Related

- [`Center-Windows` in the Window module reference](../../../modules/window.md#center-windows) - parameters, usage and behaviour
- [Window configuration guides](README.md) - every guide for this module
