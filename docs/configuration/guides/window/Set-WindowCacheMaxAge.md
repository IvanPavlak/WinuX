# Set-WindowCacheMaxAge

Sets the maximum age for the window cache, configuring how long the window enumeration cache remains valid.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Set-WindowCacheMaxAge -MaxAgeMs 100
```

## Related

- [`Set-WindowCacheMaxAge` in the Window module reference](../../../modules/window.md#set-windowcachemaxage) - parameters, usage and behaviour
- [Window configuration guides](README.md) - every guide for this module
