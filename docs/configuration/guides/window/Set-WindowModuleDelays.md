# Set-WindowModuleDelays

Sets Window module timing configuration values.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Set-WindowModuleDelays -Delays @{ FocusSettleMs = 10; WindowRestoreMs = 10 }
```

## Related

- [`Set-WindowModuleDelays` in the Window module reference](../../../modules/window.md#set-windowmoduledelays) - parameters, usage and behaviour
- [Window configuration guides](README.md) - every guide for this module
