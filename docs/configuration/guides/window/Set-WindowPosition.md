# Set-WindowPosition

Sets the position and size of a window by handle and pixel coordinates.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Set-WindowPosition -WindowHandle $handle -X 0 -Y 0 -Width 1920 -Height 1080
```

## Related

- [`Set-WindowPosition` in the Window module reference](../../../modules/window.md#set-windowposition) - parameters, usage and behaviour
- [Window configuration guides](README.md) - every guide for this module
