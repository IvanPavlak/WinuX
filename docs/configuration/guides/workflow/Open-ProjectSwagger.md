# Open-ProjectSwagger

Opens a project's Swagger UI tab, if it has one and it is not already open.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Open-ProjectSwagger -Project "MyProject"
Open-ProjectSwagger -Project $selectedProjects -Browser "Firefox"
```

## Related

- [`Open-ProjectSwagger` in the Workflow module reference](../../../modules/workflow.md#open-projectswagger) - parameters, usage and behaviour
- [Workflow configuration guides](README.md) - every guide for this module
