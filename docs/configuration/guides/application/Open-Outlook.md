# Open-Outlook

Opens the new Outlook (Microsoft Store app) through the shared `Start-Application` launcher, using configuration-driven values from `Configuration.psd1` (`OutlookLauncherExe` plus the Outlook AppsFolder argument).

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Open-Outlook
```

## Related

- [`Open-Outlook` in the Application module reference](../../../modules/application.md#open-outlook) - parameters, usage and behaviour
- [Application configuration guides](README.md) - every guide for this module
