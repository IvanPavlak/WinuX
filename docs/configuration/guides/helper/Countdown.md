# Countdown

Displays a colored countdown timer from a given number of seconds down to zero, with an optional message shown before it begins.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
Countdown -Seconds 5
Countdown -Seconds 5 -Message "Restarting in:"
```

## Related

- [`Countdown` in the Helper module reference](../../../modules/helper.md#countdown) - parameters, usage and behaviour
- [Helper configuration guides](README.md) - every guide for this module
