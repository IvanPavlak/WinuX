# GitBranch

Creates a new branch or lists all branches. Alias: `gb`.

## Configuration Keys

This function reads no `Configuration.psd1` keys. There is nothing to configure.

## Usage

```powershell
GitBranch
GitBranch feature/my-feature
```

## Related

- [`GitBranch` in the Git module reference](../../../modules/git.md#gitbranch) - parameters, usage and behaviour
- [Git configuration guides](README.md) - every guide for this module
